# Kret Check — เอกสารส่งต่องาน

เอกสารนี้คือเอกสารส่งต่องานฉบับที่เขียนไว้เมื่อ 31 ส.ค. 2569
เก็บไว้ในนี้ตามที่ระบุในข้อ 5 ให้ Claude Code หรือคนที่มาทำต่ออ่านก่อนเริ่มงาน

> **หมายเหตุเรื่องชื่อ repo** — ชื่อปัจจุบันสะกดว่า `khokret` ซึ่งน่าจะตกตัว h ไป
> ถ้าตั้งใจให้เป็น `kohkret` ควรเปลี่ยนตอนนี้เลยก่อนที่จะมี commit เยอะ
> (ยังไม่ได้เปลี่ยนให้ เพราะการเปลี่ยนชื่อ repo กระทบ URL ของ GitHub Pages
> และควรเป็นการตัดสินใจของเจ้าของงาน — ดูข้อ 10)

---

## 0. สถานะล่าสุด — อัปเดตหลังรอบทำงานแรก

**สิ่งที่เปลี่ยนไปจากตอนเขียนเอกสารนี้**

ตอนเปิด repo ครั้งแรกพบว่ายังว่างเปล่า ไม่มี commit และไม่มีไฟล์
`kretcheck-site.html`, `kretcheck-sitemap.html`, `kretcheck-brand.html`, `kretcheck-logo.html`
ที่ระบุไว้ในข้อ 9 อยู่เลย จึงไม่มี CSS/JS inline ให้แยกออกมาตามข้อ 7.1
งานรอบนี้จึงสร้างเว็บขึ้นใหม่ตามสเปกในเอกสารนี้ทั้งหมด ซึ่งได้ผลปลายทางเหมือนกัน
คือมีไฟล์ CSS/JS แยกและสีทุกสีมาจาก `tokens.css` ที่เดียว

**ทำแล้ว**

- [x] โครงสร้างไฟล์ตามข้อ 5
- [x] `assets/css/tokens.css` — สี ฟอนต์ ระยะห่าง breakpoint อยู่ที่นี่ที่เดียว
- [x] `assets/css/main.css` — mobile first เขียนด้วย `min-width` ล้วน
- [x] `assets/js/main.js` — theme toggle, burger menu, scroll reveal, ไฮไลต์เมนูตาม section
- [x] `index.html` — ครบทั้ง 10 section ตามข้อ 4
- [x] โลโก้ 3 ขนาดเป็นไฟล์ `.svg` แยก
- [x] `article-template.html` — เทมเพลตหน้าบทความ (ข้อ 7.4)
- [x] หน้าหมวด `places/` 4 หน้า · `eat/` 4 หน้า · `plans/` 3 หน้า
- [x] `404.html`
- [x] `docs/sitemap.html` — ผังเว็บ + wireframe 4 ขนาดจอ + ตารางสี
- [x] `docs/brand.html` — คำอธิบายชื่อ โลโก้ สี ฟอนต์
- [x] หมุดบนแผนที่กดได้ เด้งป๊อบอัพ และลิงก์ต่อไปหน้าเนื้อหาในเว็บ (ดูข้อ 11)

**ยังไม่ได้ทำ**

- [ ] ใส่รูปจริง — มีช่องรออยู่ ค้นด้วยคำว่า `ใส่รูปตรงนี้`
- [ ] ไปเก็บข้อมูลจริงที่เกาะเกร็ด แล้วแทนที่ตัวเลขตัวอย่างทั้งหมด (ดูข้อ 6.7)
- [ ] หน้า `เกี่ยวกับเรา` และ `ติดต่อ`
- [ ] หน้าผลค้นหา และหน้าแท็ก
- [ ] deploy ด้วย GitHub Pages

---

## 1. โปรเจกต์นี้คืออะไร

เว็บบล็อกท่องเที่ยวสำหรับงานส่วนที่ 2 ของวิชา หัวข้อที่จับสลากได้คือ
**"เกาะเกร็ด ชุมชนมอญและคาเฟ่ชิคๆ (นนทบุรี)"**

โจทย์จากอาจารย์: สร้างเว็บบล็อกท่องเที่ยวประจำจุดหมายปลายทางผ่านแพลตฟอร์มฟรี
โดยออกแบบโครงสร้างหน้าเว็บ โลโก้ และโทนสีที่ตอบโจทย์พฤติกรรมของ Persona ที่วิเคราะห์ไว้ในงานส่วนที่ 1

**Persona:** Gen Z อายุ 18–26 ปี
**หลักการหลัก:** mobile first แล้วขยายขึ้นไปหาจอใหญ่ ไม่ใช่ย่อลงมาจากจอคอม

---

## 2. แบรนด์

### ชื่อ

- **ชื่อทางการ:** `Kret Check` — ใช้ใน README, URL, handle, และเอกสารทุกชนิด
- **ในโลโก้:** เขียนว่า `KRET CHECK!` ได้ เครื่องหมาย `!` เป็นลูกเล่นทางกราฟิกเท่านั้น **ห้ามใส่ใน URL หรือชื่อไฟล์**
- **บรรทัดคู่:** ต้องมี `เกาะเกร็ด · ชุมชนมอญ นนทบุรี` ใต้โลโก้เสมอในบริบทที่คนยังไม่รู้จักแบรนด์

### ความหมาย

`Kret` = เกาะเกร็ด ตัดเหลือพยางค์เดียว
`Check` = ทำงานสองชั้น — **เช็กอิน** (ไปถึงแล้วถ่ายรูปลงโซเชียล) และ **เช็ก** (ตรวจสอบข้อมูลก่อนไป)

### โลโก้ — เกาะกลางน้ำ

เล่าเรื่องเดียว: เกาะกลางแม่น้ำที่มีวัดอยู่บนนั้น

| ชิ้น | คืออะไร |
|---|---|
| โดมสีเขียว | ตัวเกาะเกร็ด |
| ยอดแหลมบนโดม | เจดีย์เอียงมุเตา |
| เส้นสองเส้นล่าง | แม่น้ำเจ้าพระยา (สื่อว่าต้องนั่งเรือ) |
| จุดเหลืองมุมขวาบน | แดดริมน้ำ — ตัดออกเมื่อเล็กกว่า 40px |

ไฟล์จริงอยู่ที่ `assets/logo/` แยกเป็น `logo-full.svg`, `logo-circle.svg`, `logo-small.svg`

---

## 3. Design tokens

อยู่ใน `assets/css/tokens.css` ที่เดียว

### สี — ห้าสี ห้ามเพิ่มสีนอกชุดนี้

| Token | Hex | ที่มาจากของจริง | ใช้ตรงไหน |
|---|---|---|---|
| Mother Earth | `#513229` | ดินเผาที่ยังไม่เคลือบ จากหมู่บ้านกวานอาม่าน | สีหลัก · navbar · hero · footer · พื้นหลังโหมดกลางคืน |
| Moselle Green | `#2D4A35` | ต้นไม้ริมน้ำและร่มเงาในวัด | ตัวเกาะในโลโก้ · ส่วนสิ่งอำนวยความสะดวก · ปุ่ม |
| Lucky Dice | `#F4F1E2` | ดินเผาเนื้อขาวและแสงธรรมชาติ | พื้นหลังกลางวัน · ตัวหนังสือกลางคืน |
| Something Blue | `#D8EBF9` | ท้องฟ้าสะท้อนผิวน้ำ | ส่วนเรือข้ามฟากโดยเฉพาะ |
| The Bay | `#FCE6B7` | แดดบ่ายและขนมหวานมอญ | สีเน้นจุดเดียว — ปุ่ม ไอคอน สติกเกอร์ |

เฉดที่ไล่เพิ่มได้ (ไม่ใช่สีใหม่ เป็นการทำให้เข้ม/อ่อนขึ้นจากห้าสีข้างบน):
`#3E261E` `#2E1C16` (น้ำตาลเข้มขึ้น) · `#FFFDF5` `#E7E3D0` (ครีมอ่อน/เข้มขึ้น) · `#7A5A48` `#CFBFAF` (ตัวหนังสือรอง)

### คอนทราสต์ที่ต้องรักษา

ทุกคู่ตัวหนังสือ/พื้นหลังต้องได้อย่างน้อย **4.5:1** (WCAG AA)

| คู่ | ค่า |
|---|---|
| `#F4F1E2` บน `#513229` | 10.0:1 |
| `#513229` บน `#F4F1E2` | 10.0:1 |
| `#F4F1E2` บน `#2D4A35` | 8.6:1 |
| `#513229` บน `#D8EBF9` | 9.3:1 |
| `#513229` บน `#FCE6B7` | 9.3:1 |
| `#7A5A48` บน `#F4F1E2` | 5.5:1 |
| `#CFBFAF` บน `#2E1C16` | 9.1:1 |

**ห้าม** ใช้ `#2D4A35` เป็นพื้นแล้วใส่ตัวหนังสือ `#513229` (ได้แค่ราว 1.2:1)

### ฟอนต์

```
Display / heading : Mitr (500, 600, 700)
Body              : IBM Plex Sans Thai (400, 500, 600)
Label / mono      : IBM Plex Mono (400, 500)
```

โหลดจาก Google Fonts ทั้งหมด

### Breakpoints

| ช่วง | อุปกรณ์ | สิ่งที่เปลี่ยน |
|---|---|---|
| `< 700px` | มือถือ | 1 คอลัมน์ · เมนูปุ่มกดเปิด |
| `700–999px` | ไอแพดแนวตั้ง | 2 คอลัมน์ · ยังใช้เมนูปุ่มกดเปิด |
| `1000–1199px` | ไอแพดแนวนอน | 4 คอลัมน์ · เมนูกางเต็มแถบ (ชิดกัน) · ตัวหนังสือ 16.5px |
| `1200px +` | คอม | 4 คอลัมน์ · ระยะห่างกว้างขึ้น · ตัวหนังสือ 17px |

---

## 4. โครงสร้างเว็บ

หน้าแรกเป็น single page เลื่อนยาว เมนูกดแล้ว scroll ไปหา section

| # | Section id | เนื้อหา |
|---|---|---|
| 1 | `nav` | โลโก้ · เมนู 6 อัน · ปุ่มสลับโหมดกลางวัน/กลางคืน |
| 2 | `#top` | Hero — พาดหัว · สติกเกอร์ 3 อัน · ปุ่ม 2 ปุ่ม · ช่องรูป (เฉพาะจอ ≥1000px) |
| 3 | `.facts` | แถบตอบ 3 คำถาม — ไกลไหม แพงไหม งบเท่าไร |
| 4 | `#plans` | แพลนเที่ยว 3 แบบ (มือถือเลื่อนซ้ายขวา / คอมเป็น grid) |
| 5 | `#places` | เที่ยวไหนดี 4 จุด |
| 6 | `#eat` | กินอะไรดี 4 อย่าง |
| 7 | `#ferry` | เรือข้ามฟาก — การ์ดตั๋ว 2 เส้นทาง + 4 ขั้นตอน (พื้นสีฟ้า) |
| 8 | `#fac` | สิ่งอำนวยความสะดวก 8 หัวข้อ (พื้นสีเขียว) |
| 9 | `#posts` | โพสต์ล่าสุด 3 เรื่อง |
| 10 | `footer` | ชวนติดตาม · โซเชียล |

---

## 5. โครงสร้างไฟล์ใน repo

```
khokret-travel-web/
├── README.md
├── HANDOFF.md              ← ไฟล์นี้
├── index.html
├── article-template.html   ← เทมเพลตหน้าบทความ ก๊อปไปทำหน้าใหม่
├── 404.html
├── assets/
│   ├── css/
│   │   ├── tokens.css      ← ตัวแปรสี ฟอนต์ breakpoint
│   │   └── main.css
│   ├── js/
│   │   └── main.js         ← theme toggle · burger menu · scroll reveal
│   ├── img/
│   └── logo/
│       ├── logo-full.svg
│       ├── logo-circle.svg
│       └── logo-small.svg  ← เวอร์ชันตัดจุดแดดออก
├── places/
├── eat/
├── plans/
└── docs/
    ├── sitemap.html        ← ผังเว็บ + wireframe 4 ขนาดจอ
    └── brand.html          ← คำอธิบายชื่อ โลโก้ สี
```

---

## 6. กฎที่ต้องรักษาไว้ตลอด

1. **ห้ามเพิ่มสีนอกห้าสี** ถ้าต้องการเฉดใหม่ ให้ไล่จากห้าสีเดิม
2. **ทุกคู่สีตัวหนังสือ/พื้นหลังต้องได้อย่างน้อย 4.5:1** เช็กก่อน commit ทุกครั้ง
3. **สีของ navbar, hero, ส่วนสิ่งอำนวยความสะดวก และ footer ถูกล็อกไว้ตายตัว** ไม่เปลี่ยนตามโหมดกลางวัน/กลางคืน — ยิ่งมีชิ้นส่วนที่สลับสีน้อย โอกาสพังยิ่งน้อย
4. **ต้องกำหนด `background` ที่ทั้ง `html` และ `body`** ไม่งั้นโหมดมืดจะพังแบบที่เคยเจอมาแล้ว
5. **ห้ามใช้ `-webkit-text-stroke` กับพาดหัว** เคยลองแล้วอ่านไม่ออก
6. **mobile first** เขียน CSS ฐานสำหรับมือถือ แล้วใช้ `min-width` ขยายขึ้น
7. **ข้อมูลค่าเรือ เวลา ราคา ยังเป็นตัวอย่าง** ห้าม deploy จริงจนกว่าจะไปเช็กของจริงมา ตอนนี้เขียนแบบกว้าง ๆ ไว้ เช่น "ค่าเรือหลักหน่วย"
8. **กล่องสรุปในหน้าบทความต้องอยู่บนสุด** ก่อนเนื้อหา — ราคา เวลาเปิด พิกัด เพราะคนครึ่งหนึ่งอ่านแค่กล่องนี้แล้วปิด

---

## 7. ลำดับงานที่เหลือ

1. ~~แยก CSS/JS ออกจาก HTML~~ ✅
2. ~~สร้าง `tokens.css`~~ ✅
3. ~~export โลโก้เป็นไฟล์ `.svg` แยก 3 ขนาด~~ ✅
4. ~~ทำเทมเพลตหน้าบทความ~~ ✅
5. ~~ทำหน้าหมวด เที่ยวไหนดี และ กินอะไรดี~~ ✅
6. **ใส่รูปจริง** — ค้นด้วยคำว่า `ใส่รูปตรงนี้` แล้วแทนที่ `<div class="slot">` ด้วย `<img>`
7. **ไปเก็บข้อมูลจริงที่เกาะเกร็ด** แล้วแทนที่ตัวเลขตัวอย่างทั้งหมด แล้วค่อยลบ `.draft-note` ออก
8. ~~ทำหน้า 404~~ ✅ · หน้าผลค้นหายังไม่ได้ทำ
9. **deploy ด้วย GitHub Pages** — ดูข้อ 10

---

## 8. วิธีทำหน้าบทความใหม่

1. ก๊อป `article-template.html` ไปไว้ใน `places/`, `eat/` หรือ `plans/`
2. เปลี่ยน path จาก `assets/...` เป็น `../assets/...` และ `index.html` เป็น `../index.html`
3. แทนที่ทุกจุดที่เขียนว่า `[[ ... ]]`
4. ลบบล็อกที่ไม่ได้ใช้ทิ้ง อย่าปล่อยกล่องว่างไว้
5. เพิ่มการ์ดลิงก์ไปหน้าใหม่ใน `index.html`

**กล่องสรุป `.summary` ต้องอยู่บนสุดก่อนเนื้อหาเสมอ** ห้ามย้ายลงล่าง (ข้อ 6.8)

---

## 9. เรื่อง deploy

ก่อนกด deploy ต้องเก็บข้อมูลจริงมาก่อนตามข้อ 6.7
ตอนนี้ทุกหน้าที่มีตัวเลขจะมีกล่อง `.draft-note` เตือนไว้ ให้ลบกล่องพวกนั้นออกเมื่อข้อมูลจริงเข้าที่แล้ว

เวลาเปิด GitHub Pages เว็บจะอยู่ที่ `https://<user>.github.io/<repo>/`
**ถ้าเปลี่ยนชื่อ repo ต้องแก้ `<base href>` ใน `404.html` ด้วย**
เพราะ GitHub Pages เสิร์ฟ `404.html` แทนทุก path ที่หาไม่เจอ ไม่ว่าจะลึกแค่ไหน
path แบบ `../` จึงใช้ไม่ได้ในไฟล์นั้น

---

## 10. เรื่องที่ยังต้องตัดสินใจ

- **ชื่อ repo** `khokret` vs `kohkret` — ยังไม่ได้เปลี่ยน เพราะกระทบ URL ของ GitHub Pages
  และ `<base href>` ใน `404.html` ถ้าจะเปลี่ยน ควรเปลี่ยนก่อน deploy
- **ลิงก์โซเชียลใน footer** ยังชี้กลับหน้าแรกอยู่ ต้องใส่ URL จริงเมื่อมีบัญชีแล้ว
- **หน้าผลค้นหา** ถ้าเว็บเป็น static ล้วน ต้องเลือกว่าจะใช้วิธีไหน (ค้นฝั่ง client ด้วย JSON index หรือส่งไป Google)

---

## 11. แผนที่กดได้ — ป๊อบอัพจุดบนแผนที่

### ตอนนี้ทำงานยังไง

หมุดตัวเลข 40 จุดใน `map.html` กดได้ทั้งเมาส์ นิ้ว และคีย์บอร์ด
กดแล้วเด้งกล่องข้อมูลที่มีชื่อไทย ชื่ออังกฤษ เส้นทางที่จุดนั้นสังกัด
และปุ่มลิงก์ไปหน้าเนื้อหาในเว็บ เช่น หมุด 31 วัดปรมัยยิกาวาส
ลิงก์ไป `places/wat-poramai.html`, `places/leaning-pagoda.html`
และ `places/riverside-market.html`

เลขในรายชื่อใต้แผนที่กดได้เหมือนกัน กดแล้วเลื่อนกลับขึ้นไปหาหมุดแล้วเปิดกล่องให้

| ไฟล์ | หน้าที่ |
|---|---|
| `assets/js/map.js` | ข้อมูล 40 จุด + ตรรกะเปิดปิดกล่อง |
| `assets/css/map.css` | สไตล์กล่อง `.poi` และหมุด `.pin` |
| `map.html` | ใส่ `data-mapsheet`, `data-map-scroll`, `data-map-legend` ไว้ให้ JS เกาะ |

**กติกาที่ต้องรักษาไว้**

- พิกัดหมุดไม่ได้เก็บซ้ำใน `map.js` แต่อ่านจาก `<circle>` ใน SVG ตรง ๆ
  ย้ายหมุดในแผนที่แล้วกล่องตามไปเอง ไม่ต้องแก้สองที่
- ไม่มี JS แล้วแผนที่กลับไปเป็นภาพนิ่ง รายชื่อ 40 จุดใต้แผนที่ยังอ่านได้ครบเหมือนเดิม
- ชั้นตกแต่งใน SVG (รอยพับกระดาษ ป้ายชื่อ ตัวคน) ตั้ง `pointer-events: none` ไว้
  **ห้ามเอาออก** ไม่งั้นหมุดที่อยู่ใต้รอยพับจะกดไม่ติด
- จอต่ำกว่า 700px กล่องเป็นแผ่นเลื่อนขึ้นจากขอบล่าง เพราะแผนที่เลื่อนแนวนอนได้
  ถ้าผูกกล่องไว้กับหมุด กล่องจะวิ่งหนีออกนอกจอตอนคนลากแผนที่

### เพิ่มหรือแก้ข้อมูลจุด

แก้ที่ตาราง `POIS` ใน `assets/js/map.js` ที่เดียว
ตอนนี้ `note` ของหลายจุดยังใช้คำอธิบายกลางของเส้นทาง เพราะยังไม่ได้ลงพื้นที่ (ข้อ 6.7)
เก็บข้อมูลจริงมาแล้วให้ใส่ `note` รายจุด และเพิ่ม `links` ชี้ไปหน้าที่เขียนถึงจุดนั้น
เวลาแก้ต้องแก้ทั้งใน `POIS` และในรายชื่อใต้แผนที่ใน `map.html` ให้ตรงกัน

### ถ้าจะเปลี่ยนไปใช้แผนที่ที่เป็นไฟล์ภาพ

โครงป๊อบอัพชุดนี้ไม่ผูกกับ SVG ที่วาดเอง ใช้กับภาพก็ได้ วิธีคือ

1. วางไฟล์ภาพไว้ที่ `assets/img/`
2. ใน `map.html` แทนที่เนื้อในของ `<svg class="map">` ด้วย
   `<image href="assets/img/ชื่อไฟล์.png" x="0" y="0" width="1400" height="980"/>`
   แล้วปรับ `viewBox` ให้เป็นสัดส่วนเดียวกับภาพ
3. เก็บ `<g id="pins">` ไว้ แล้วขยับ `cx`/`cy` ของแต่ละหมุดให้ตรงตำแหน่งบนภาพ
4. ไม่ต้องแก้ `map.js` เลย

**ก่อนทำต้องเช็กเรื่องลิขสิทธิ์ก่อน** แผนที่ท่องเที่ยวที่วาดเป็นภาพประกอบ
เป็นงานที่มีเจ้าของ การเอาไฟล์ของคนอื่นมาแปะบนเว็บที่เผยแพร่จริง
ต้องได้รับอนุญาตจากเจ้าของก่อน หรือไม่ก็ต้องเป็นภาพที่เราวาดเอง
เหตุผลนี้คือเหตุผลที่แผนที่ปัจจุบันเลือกวาดเป็น SVG เอง
ซึ่งได้ของแถมมาด้วยคือย่อขยายไม่แตก ไฟล์เล็ก สีมาจาก `tokens.css` ที่เดียว
และตัวหนังสือในแผนที่เป็นข้อความจริงที่ screen reader อ่านได้ ไม่ใช่พิกเซล
